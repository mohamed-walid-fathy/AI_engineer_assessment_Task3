pandas>=2.0
openpyxl>=3.1
streamlit>=1.35
pytest>=7.4
google-genai>=1.0
python-dotenv>=1.0
# ML stack pinned exactly: transformers' import chain requires a matching
# torchvision, and unpinned upgrades have broken embedding load in the past.
sentence-transformers==6.0.1
transformers==5.17.0
torchvision==0.29.0
